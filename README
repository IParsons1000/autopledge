auto-pledge
===========

automatically sandbox syscalls available to vulnerable programs

auto-pledge automatically detects and whitelists syscalls used by a program
at runtime, restricting potential injected shellcode

Usage
-----

```
$ LD_PRELOAD=./libautopledge.so <program>
```

Build
-----

```
make all
make test
```

Install
-------

```
sudo make install
```
